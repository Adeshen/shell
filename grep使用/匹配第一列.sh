
ps aux | grep "^  904\b"

# ps aux：列出当前所有正在运行的进程的详细信息。
# grep：搜索输入中与给定模式匹配的行。
# ^  904：正则表达式，匹配以"904"开头的行。注意，这里的空格是必需的，以避免匹配到包含"904"的其他列。
# \b：正则表达式，表示单词边界。这确保了只匹配PID为904的进程，而不是包含"904"的其他PID。
# 最终，您将只看到具有PID为904的进程的行。